/**
 * Layout Matrix
 * 坐标点 [XX,XX]
 *
 */
/**
 *
 * @param nodesMapByFiles 按文件名group的diff节点
 *
 * @return 文件名对应的坐标边界 上下左右 ([XX,XX],[XX,XX],[XX,XX],[XX,XX])
 */
function layoutFilePanels(nodesMapByFiles){

}

/**
 * 对存在external也就是文件间的节点layout
 * @param nodesMapByFiles
 */
function layoutExternalEdges(nodesMapByFiles){

}

/**
 * 对存在external也就是文件间的节点，相链接的节点layout
 * @param nodesMapByFiles
 */
function layoutNodesLinkedToExternalEdges(nodesMapByFiles){

}

/**
 * 对存在external也就是文件间的节点，相链接的节点layout
 * @param nodesMapByFiles
 */
function layoutNodesInternalNodes(nodesMapByFiles){

}



