import { Surface } from "../surface";
import { Vertex } from "../../core/model/graph";
import { BrowserElement } from "../../ui-core/browser-ui-renderer/util";
/**
 * Unused currently.
 * @internal
 */
export declare function renderEndpointFromTag(element: BrowserElement, data: any, surface: Surface, obj: Vertex): void;
